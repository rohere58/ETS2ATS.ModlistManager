## ETS2 / ATS Modlist Manager v0.1.19.1

### Fixes
- Localization: Main window labels are translated correctly again (incl. quick buttons, combobox labels, and footer header for modlist info).
- Localization: About and Options dialog titles now follow the selected language.

### Downloads
- Multi-File (recommended): `modlist-manager-0.1.19.1-win-x64.zip`
- Single-File (self-contained): `modlist-manager-0.1.19.1-self-contained-win-x64.zip`

### SHA256
Each ZIP has a matching `.sha256` file. Verify with PowerShell:

```powershell
Get-FileHash .\modlist-manager-0.1.19.1-win-x64.zip -Algorithm SHA256
Get-Content .\modlist-manager-0.1.19.1-win-x64.zip.sha256
```
